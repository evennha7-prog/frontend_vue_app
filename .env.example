# =============================================================================
# Frontend Environment Configuration Template
# Copy this file to .env or .env.local for local overrides.
# =============================================================================

# Base URL for the ASP.NET Core backend API.
# In local development with Vite dev server proxying, leave blank or use /api.
# In production, set to your deployed backend URL (e.g., https://your-api.runasp.net/api).
VITE_API_URL=
